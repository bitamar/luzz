apiVersion: v1
kind: Service
metadata:
  name: {{ include "luz-api.fullname" . }}
  labels:
    {{- include "luz-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "luz-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP


