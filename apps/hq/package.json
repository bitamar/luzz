{
  "name": "@apps/hq",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "sync": "svelte-kit sync",
    "lint": "pnpm -w lint",
    "typecheck": "svelte-check --fail-on-warnings",
    "test": "pnpm -w test:fe",
    "format": "prettier --config ../prettier.config.mjs --write ."
  },
  "dependencies": {
    "@sveltejs/kit": "^2.7.0",
    "svelte": "^5.0.0"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.8",
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0",
    "vite": "^6.3.0"
  }
}
